import React from "react";
import { AlignRight } from "react-feather";

const NavMenu = () => {
  return (
    <button type="button">
      <AlignRight size={50} strokeWidth={1.2} />
    </button>
  );
};

export default NavMenu;
